name: Update Profile

on:
  push:
    branches:
      - master

  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Netlify Build
        run: |  
            curl -X POST -d {} https://api.netlify.com/build_hooks/62b454407c9a310e317837a5