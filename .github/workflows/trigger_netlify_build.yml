name: Update Profile

on:
  # push:
  #   branches:
  #     - master

  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Netlify Build
        run: |  
            curl -X POST -d {} ${{secrets.NETLIFY_WEBHOOK}}